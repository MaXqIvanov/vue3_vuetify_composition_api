import { createStore } from 'vuex'
import UserModule from './UsersModule'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    users: UserModule,
  }
})
